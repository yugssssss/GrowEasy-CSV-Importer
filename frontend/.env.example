# Base URL of the deployed backend API.
# Local dev default is http://localhost:5000 (used automatically if unset).
# In production (e.g. Vercel), set this to your Render/Railway backend URL:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=http://localhost:5000
